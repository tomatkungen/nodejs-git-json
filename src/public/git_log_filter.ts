import { GitLogs } from "../types/git_types";
import { CONFIG, Config } from "./../types/config.types";

export const git_log_filter = async (path: string = './', config: Config = CONFIG): Promise<GitLogs> => {

    return [];    
}