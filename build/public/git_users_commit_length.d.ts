import { Config } from "../types/config.types";
import { GitUsersCommitLength } from "../types/git_types";
export declare const git_users_commit_length: (path?: string, config?: Config) => Promise<GitUsersCommitLength>;
